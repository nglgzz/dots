[branch "master"]
  remote = upstream
  pushRemote = origin
  merge = master

[branch "trunk"]
  remote = upstream
  pushRemote = origin
  merge = trunk

[branch "main"]
  remote = upstream
  pushRemote = origin
  merge = main

